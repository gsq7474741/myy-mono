# 此工作流将部署构建好的 h5 应用到阿里云 OSS
# 仅在成功构建后由 build 工作流触发执行

name: 部署 myy-app 生产版本到阿里云 OSS

on:
  push:
    branches: [ "develop" ]
    paths:
      - 'apps/myy-app/**'

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: app-aly-deploy-production
    steps:
    - uses: actions/checkout@v4
    
    - name: 安装 pnpm
      uses: pnpm/action-setup@v2
      with:
        version: 8.10.0

    - name: 设置 Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20.x'
        cache: 'pnpm'

    # 安装前端依赖
    - name: 安装前端依赖
      working-directory: ./apps/myy-app
      run: pnpm install --no-frozen-lockfile --filter=myy-app
    
    # 构建前端
    - name: 构建前端
      working-directory: ./apps/myy-app
      env:
        NODE_ENV: production
        TARO_APP_BUILD_ENV: production
        TARO_APP_AI_API_KEY: ${{ secrets.TARO_APP_AI_API_KEY }}
        TARO_APP_AI_API_BASE_URL: ${{ vars.TARO_APP_AI_API_BASE_URL }}
        TARO_APP_API_BASE_URL: ${{ vars.TARO_APP_API_BASE_URL }}
      run: pnpm run build:h5
    
    # 安装 tsx 运行时依赖
    - name: 安装 tsx
      working-directory: ./apps/myy-app
      run: pnpm install tsx @types/node ali-oss @types/ali-oss

    # 上传到阿里云 OSS
    - name: 上传到阿里云 OSS
      env:
        ALIBABA_CLOUD_ACCESS_KEY_ID: ${{ secrets.ALIBABA_CLOUD_ACCESS_KEY_ID }}
        ALIBABA_CLOUD_ACCESS_KEY_SECRET: ${{ secrets.ALIBABA_CLOUD_ACCESS_KEY_SECRET }}
        PAGE_OSS_BUCKET: ${{ vars.PAGE_OSS_BUCKET }}
        PAGE_OSS_REGION: ${{ vars.PAGE_OSS_REGION }}
        PAGE_OSS_PATH: ${{ vars.PAGE_OSS_PATH }}
      run: |
        cd apps/myy-app
        npx tsx upload.ts
